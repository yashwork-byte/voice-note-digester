# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Virtual env
.venv/

# Secrets (never commit real tokens)
.env
.env.local

# Local data (synthesized audio can get large; training data regenerates
# deterministically from scripts/generate_training_data.py)
data/eval_audio/
data/processed/
data/train/
data/notes.db
data/models/

# macOS
.DS_Store

# Personal design-decision journal — local only, never push
decisions.md
